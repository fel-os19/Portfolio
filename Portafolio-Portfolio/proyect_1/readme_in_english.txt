# User Authentication System with JSON (Python CLI)

This is a simple user authentication system built with Python. It runs in the terminal and uses a `.json` file to store and validate user data such as name, email, and password.

## 🔧 Features

- User login and validation
- Email and password check
- Data stored in a `user.json` file
- Simple logic with clear user feedback
- Loop for repeated attempts until correct input

## 🧠 Technologies Used

- Python 3.13
- JSON (built-in module)
- Console I/O
- Basic control flow (loops, conditionals)
